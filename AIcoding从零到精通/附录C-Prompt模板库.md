# 附录C：Prompt模板库

> 25+个高质量Prompt模板，覆盖常见开发场景。直接复制、修改尖括号中的内容即可使用。

---

## 一、写代码

### 1. 写API接口

```
帮我用Python Flask写一个<功能描述>接口。
要求：
- 支持的HTTP方法：<GET/POST/PUT/DELETE>
- 请求参数：<列出参数名和类型>
- 返回JSON格式：<描述返回结构>
- 包含错误处理（如参数缺失返回400）
- 代码风格清晰，加注释
```

### 2. 写前端页面

```
帮我写一个HTML页面，功能是<描述>。
要求：
- 使用纯HTML+CSS+JS，不需要框架
- 样式简洁干净，使用Bootstrap或类似风格
- 响应式设计，手机和电脑都能看
- 包含<具体交互：表单/列表/按钮等>
```

### 3. 写Python脚本

```
帮我写一个Python脚本，功能是<描述>。
要求：
- 使用Python 3.12+
- 处理好文件路径（跨平台兼容）
- 包含错误处理（文件不存在、权限不足等）
- 打印有意义的进度提示
- 加注释说明关键逻辑
```

### 4. 写正则表达式

```
帮我写一个正则表达式，用于匹配<描述要匹配的文本模式>。
要求：
- 给出正则表达式
- 解释每个部分的含义
- 给出3个匹配示例和3个不匹配示例
```

### 5. 写Shell脚本

```
帮我写一个Shell脚本，功能是<描述>。
要求：
- 兼容Windows PowerShell和Linux Bash（给出两种版本）
- 包含错误处理
- 打印有意义的提示信息
```

---

## 二、调试修复

### 6. 分析错误

```
我运行代码时遇到了以下错误：
```
<粘贴完整错误信息>
```
我的代码是：
```python
<粘贴相关代码>
```
请帮我分析错误原因，并给出修复方案。
```

### 7. 修复Bug

```
以下代码有一个Bug：<描述Bug现象>
```python
<粘贴有Bug的代码>
```
请帮我找出Bug并修复，同时解释Bug的原因。
```

### 8. 优化性能

```
以下代码运行很慢，请帮我分析性能瓶颈并优化：
```python
<粘贴代码>
```
请给出优化后的代码，并解释每一步优化的原因。
```

---

## 三、代码审查

### 9. 代码审查

```
请帮我审查以下代码，按五个维度分析：
1. Bug：有没有逻辑错误
2. 风格：代码风格是否一致
3. 性能：有没有明显的性能问题
4. 安全：有没有安全漏洞（SQL注入、XSS等）
5. 可读性：变量名、函数名是否清晰，是否需要更多注释

```python
<粘贴代码>
```
```

### 10. 安全审查

```
请帮我检查以下代码的安全问题：
- 是否有硬编码的密钥或密码
- 是否有SQL注入风险
- 是否有XSS风险
- 是否有敏感信息泄露
- 是否有不安全的依赖

```python
<粘贴代码>
```
```

---

## 四、测试

### 11. 写单元测试

```
请帮以下函数写单元测试，覆盖以下场景：
- 正常输入
- 边界值（最小值、最大值、空值）
- 异常输入（错误类型、None等）

```python
<粘贴函数代码>
```
使用Python的unittest框架。
```

### 12. 写测试用例

```
请帮我为以下功能设计测试用例，用表格形式列出：
| 测试场景 | 输入 | 预期输出 | 优先级 |
- 覆盖正常流程
- 覆盖异常流程
- 覆盖边界条件

功能描述：<描述功能>
```

---

## 五、文档

### 13. 写README

```
请帮我给这个项目写一个README.md，包含以下内容：
- 项目名称和简介
- 安装方法
- 使用方法（带示例）
- 项目结构说明
- 许可证

项目描述：<一句话描述>
技术栈：<列出技术栈>
```

### 14. 写代码注释

```
请帮我给以下代码添加清晰的注释，解释每一段逻辑：
```python
<粘贴代码>
```
用中文注释。
```

### 15. 写API文档

```
请帮我给以下API接口写文档，每个接口包括：
- URL和方法
- 请求参数（名称、类型、必填、说明）
- 返回格式（JSON示例）
- 错误码说明

```python
<粘贴API代码>
```
```

---

## 六、重构

### 16. 提取函数

```
以下代码有重复的逻辑，请帮我提取成独立的函数：
```python
<粘贴代码>
```
保持功能不变，只改结构。
```

### 17. 简化代码

```
以下代码逻辑太复杂，请帮我简化，提高可读性：
```python
<粘贴代码>
```
保持功能不变，用更简洁的方式实现。
```

### 18. 统一重命名

```
请帮我重构以下代码中的变量名和函数名，使其更清晰、更符合Python命名规范：
```python
<粘贴代码>
```
保持功能不变。
```

---

## 七、学习理解

### 19. 解释代码

```
请用大白话解释以下代码做了什么，我是一个编程新手：
```python
<粘贴代码>
```
请逐行解释，用比喻来帮助理解。
```

### 20. 对比技术方案

```
我需要实现<描述需求>，目前考虑了两种方案：
方案A：<描述方案A>
方案B：<描述方案B>
请帮我对比这两种方案的优缺点，并给出建议。
```

### 21. 代码翻译

```
请把以下<语言A>代码翻译成Python：
```<语言A>
<粘贴代码>
```
保持相同的功能和逻辑。
```

---

## 八、项目创建

### 22. 创建项目

```
帮我创建一个<项目类型>项目，功能包括：
1. <功能1>
2. <功能2>
3. <功能3>
技术栈用<技术栈>，请给出完整的项目结构和所有文件内容。
```

### 23. 数据库设计

```
我需要设计一个数据库表结构，用于存储<描述数据>。
要求：
- 列出所有表名和字段
- 标注主键、外键、索引
- 给出建表SQL语句
- 解释每个表之间的关系
```

### 24. 架构设计

```
请帮我设计一个<项目类型>的架构，包括：
- 技术选型建议
- 目录结构
- 模块划分
- 数据流
- 关键接口
```

---

## 九、通用

### 25. Few-shot（给示例）

```
请按照以下格式输出：
示例输入：<输入示例>
示例输出：<输出示例>
现在，请处理以下输入：<你的输入>
```

---

> **使用技巧**：每个模板中`<...>`的部分替换成你自己的内容。模板只是起点，你可以在模板基础上继续添加具体约束和需求。