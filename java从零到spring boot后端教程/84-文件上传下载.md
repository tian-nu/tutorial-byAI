# 84-文件上传下载

> 💡 你去照相馆洗照片：把胶卷给店员（上传），店员冲洗出来（存储），你来取照片（下载）。Web 应用的文件操作同理——用户把文件传给你，你存到某个地方，用户再来下载。本章讲清楚这个"收-存-发"的全流程。

---

## 本章目标
- 使用 `MultipartFile` 接收文件上传
- 实现文件下载（`ResponseEntity<Resource>`）
- 理解三种文件存储方案及其适用场景
- 配置静态资源映射
- 实现文件大小限制与类型校验

---

## 84.1 文件上传——MultipartFile

### 配置上传限制

```yaml
spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
```

### Controller

```java
@RestController
@RequestMapping("/api/files")
public class FileController {

    private final FileService fileService;

    public FileController(FileService fileService) {
        this.fileService = fileService;
    }

    @PostMapping("/upload")
    public Result<Map<String, String>> upload(
            @RequestParam("file") MultipartFile file) {
        String fileUrl = fileService.store(file);
        return Result.success(Map.of(
                "fileName", file.getOriginalFilename(),
                "fileUrl", fileUrl,
                "fileSize", String.valueOf(file.getSize())
        ));
    }
}
```

### MultipartFile 常用方法

| 方法 | 返回值 | 说明 |
|------|--------|------|
| `getOriginalFilename()` | String | 原始文件名 |
| `getContentType()` | String | MIME 类型 |
| `getSize()` | long | 文件字节数 |
| `getBytes()` | byte[] | 全部内容（小文件） |
| `getInputStream()` | InputStream | 流（大文件） |
| `isEmpty()` | boolean | 是否为空 |

---

## 84.2 文件存储——三种方案

### 方案一：本地磁盘（开发/小项目）

```java
@Service
public class FileService {

    @Value("${file.upload-dir:uploads}")
    private String uploadDir;

    public String store(MultipartFile file) {
        if (file.isEmpty()) {
            throw new BusinessException("文件为空");
        }

        String originalName = file.getOriginalFilename();
        String extension = "";
        if (originalName != null && originalName.contains(".")) {
            extension = originalName.substring(originalName.lastIndexOf("."));
        }
        String storedName = UUID.randomUUID() + extension;

        try {
            Path uploadPath = Paths.get(uploadDir);
            if (!Files.exists(uploadPath)) {
                Files.createDirectories(uploadPath);
            }
            Path targetPath = uploadPath.resolve(storedName);
            Files.copy(file.getInputStream(), targetPath,
                    StandardCopyOption.REPLACE_EXISTING);
            return "/files/" + storedName;
        } catch (IOException e) {
            throw new RuntimeException("文件存储失败", e);
        }
    }
}
```

关键点：
- 用 `UUID` 重命名文件，防止冲突和安全问题
- `Files.createDirectories` 自动创建不存在的目录

### 方案二：数据库 BLOB（不推荐）

仅适合极小文件（如头像缩略图）。大文件会让数据库膨胀、备份极慢。

### 方案三：云存储（生产推荐）

阿里云 OSS / AWS S3 / MinIO：

```java
@Service
public class MinioFileService {

    private final MinioClient minioClient;

    public MinioFileService(
            @Value("${minio.endpoint}") String endpoint,
            @Value("${minio.access-key}") String accessKey,
            @Value("${minio.secret-key}") String secretKey) {
        this.minioClient = MinioClient.builder()
                .endpoint(endpoint)
                .credentials(accessKey, secretKey)
                .build();
    }

    public String store(MultipartFile file) {
        String objectName = UUID.randomUUID() + "-" + file.getOriginalFilename();
        minioClient.putObject(PutObjectArgs.builder()
                .bucket("my-bucket")
                .object(objectName)
                .stream(file.getInputStream(), file.getSize(), -1)
                .contentType(file.getContentType())
                .build());
        return "/files/" + objectName;
    }
}
```

---

## 84.3 文件下载

```java
@RestController
@RequestMapping("/api/files")
public class FileDownloadController {

    @Value("${file.upload-dir:uploads}")
    private String uploadDir;

    @GetMapping("/{filename}")
    public ResponseEntity<Resource> download(@PathVariable String filename) {
        try {
            Path filePath = Paths.get(uploadDir).resolve(filename).normalize();
            Resource resource = new UrlResource(filePath.toUri());

            if (!resource.exists() || !resource.isReadable()) {
                return ResponseEntity.notFound().build();
            }

            return ResponseEntity.ok()
                    .contentType(MediaType.APPLICATION_OCTET_STREAM)
                    .header(HttpHeaders.CONTENT_DISPOSITION,
                            "attachment; filename=\"" + filename + "\"")
                    .body(resource);
        } catch (MalformedURLException e) {
            return ResponseEntity.badRequest().build();
        }
    }
}
```

### `inline` vs `attachment`

| Content-Disposition | 浏览器行为 |
|---------------------|-----------|
| `inline` | 浏览器尝试直接预览 |
| `attachment` | 浏览器弹出下载对话框 |

---

## 84.4 文件类型校验——安全必备

```java
@Service
public class FileService {

    private static final Set<String> ALLOWED_TYPES = Set.of(
            "image/jpeg", "image/png", "image/gif",
            "application/pdf", "text/plain"
    );

    public String store(MultipartFile file) {
        // 1. 空文件校验
        if (file.isEmpty()) throw new BusinessException("文件为空");

        // 2. 大小校验
        if (file.getSize() > 10 * 1024 * 1024)
            throw new BusinessException("文件超过10MB");

        // 3. MIME 类型校验
        String contentType = file.getContentType();
        if (contentType == null || !ALLOWED_TYPES.contains(contentType))
            throw new BusinessException("不支持的类型: " + contentType);

        // 4. 文件名安全处理（防路径穿越）
        String originalName = file.getOriginalFilename();
        String safeName = Paths.get(originalName).getFileName().toString();

        // 5. 存储
        String storedName = UUID.randomUUID() + getExtension(safeName);
        // ...
    }
}
```

---

## 84.5 静态资源映射

```java
@Configuration
public class WebMvcConfig implements WebMvcConfigurer {

    @Value("${file.upload-dir:uploads}")
    private String uploadDir;

    @Override
    public void addResourceHandlers(ResourceHandlerRegistry registry) {
        String absolutePath = Paths.get(uploadDir).toAbsolutePath().toUri().toString();
        registry.addResourceHandler("/files/**")
                .addResourceLocations(absolutePath);
    }
}
```

---

## 84.6 小结

| 知识点 | 核心内容 |
|--------|----------|
| MultipartFile | 接收上传文件的核心接口 |
| 本地存储 | `Files.copy` + UUID 重命名 |
| 云存储 | MinIO / OSS / S3，生产推荐 |
| 文件下载 | `ResponseEntity<Resource>` + Content-Disposition |
| 类型校验 | 后端必须再次校验 MIME 和大小 |
| 路径穿越 | 用 `Paths.get().getFileName()` 防止 |
| 静态资源映射 | `addResourceHandlers` 暴露上传目录 |

---

## 84.7 自测题

**1. 用户上传文件名为 `../../../etc/passwd`。如何防止路径穿越攻击？**

**2. `Content-Disposition` 的 `inline` 和 `attachment` 有什么区别？**

**3. 设计头像上传功能：只接受 jpg/png，不超过 2MB。写出 Controller 方法和关键校验。**

---

**答案提示**：1→用 `Paths.get(originalName).getFileName().toString()` 只取文件名。2→inline 预览，attachment 下载。3→校验 `contentType` 为 image/jpeg 或 image/png；校验 `getSize()` 不超过 2MB。下一章——API 文档。